# Burd ML inference (birder photo ID + Perch sound ID).
# Deploy from the server/ directory: fly deploy --app burd-inference
#
# IMPORTANT: internal_port must be 8000 — uvicorn listens there, not 3000.

app = 'burd-inference'
primary_region = 'iad'

# ML image layers exceed the default 8GB unpack cap when uncompressed.
[rootfs]
  size_gb = 20
  persist = "restart"

# Generous swap so the Perch/XLA compile spike can't OOM-kill the process on
# the 2GB RAM org cap. Slower during the spike, but no crash loop.
swap_size_mb = 3072

[build]
  dockerfile = 'Dockerfile.fly'

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1

  [[http_service.checks]]
    grace_period = '300s'
    interval = '45s'
    method = 'GET'
    path = '/health'
    timeout = '60s'

[[vm]]
  memory = '2gb'
  cpu_kind = 'shared'
  cpus = 2
